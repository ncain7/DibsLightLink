<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/change_color"
        android:icon="@drawable/ic_action_color_white"
        app:showAsAction="ifRoom"
        android:title="@string/change_color"/>
    <item android:id="@+id/make_connection"
        android:icon="@drawable/ic_connection"
        app:showAsAction="ifRoom"
        android:title="@string/make_connection"/>
    <item android:id="@+id/configure_mode"
        android:icon="@drawable/ic_action_configure"
        app:showAsAction="ifRoom"
        android:title="@string/configure_modes"/>
</menu>